import LoadingSpinner from "@/component/LoadingSpinner/LoadingSpinner";

const loading = () => {
    return (
        <div>
            <LoadingSpinner />
        </div>
    );
};

export default loading;